"use client";
/* eslint-disable react-hooks/exhaustive-deps */

import { useEffect, useRef, useState, type PointerEvent } from "react";
import { create2048State, update2048, type Direction2048, type Game2048State } from "@/game-engine/game-2048";
import { getVisualThemeTokens } from "@/game-engine/visual-theme";
import { get2048Cells } from "./game-2048-layout";
import type { Game2048Spec } from "@/schemas/game-spec";

const directions: Record<string, Direction2048 | undefined> = { ArrowUp: "up", KeyW: "up", ArrowDown: "down", KeyS: "down", ArrowLeft: "left", KeyA: "left", ArrowRight: "right", KeyD: "right" };
const color = (value: number, accent: string) => value === 0 ? "#ffffff12" : value === 2 ? "#ffffff30" : value === 4 ? "#ffffff4a" : value <= 16 ? accent : value <= 64 ? "#8d7dff" : value <= 256 ? "#d86eff" : "#ff9e53";
export function Game2048Preview({ config }: { config: Game2048Spec }) { const [state, setState] = useState<Game2048State>(() => create2048State(config)), start = useRef<{ x: number; y: number } | null>(null), visual = getVisualThemeTokens(config.visualTheme); const reset = (running = false) => { const next = create2048State(config); next.status = running ? "running" : "ready"; setState(next); }; const move = (direction: Direction2048) => setState((current) => update2048(current, config, direction)); useEffect(() => { const down = (event: KeyboardEvent) => { const direction = directions[event.code]; if (event.code === "Space") { event.preventDefault(); setState((current) => current.status === "running" ? { ...current, status: "paused" } : current.status === "paused" ? { ...current, status: "running" } : current); } else if (direction && state.status === "running") { event.preventDefault(); move(direction); } }; const hidden = () => setState((current) => document.hidden && current.status === "running" ? { ...current, status: "paused" } : current); window.addEventListener("keydown", down); document.addEventListener("visibilitychange", hidden); return () => { window.removeEventListener("keydown", down); document.removeEventListener("visibilitychange", hidden); }; }, [config, state.status]); const end = (event: PointerEvent<HTMLDivElement>) => { if (!start.current) return; const dx = event.clientX - start.current.x, dy = event.clientY - start.current.y; start.current = null; if (Math.max(Math.abs(dx), Math.abs(dy)) < 22 || state.status !== "running") return; move(Math.abs(dx) > Math.abs(dy) ? dx > 0 ? "right" : "left" : dy > 0 ? "down" : "up"); }; const title = state.status === "won" ? `${config.title}成功` : state.status === "lost" ? `${config.title}失败` : state.status === "paused" ? "游戏已暂停" : "开始游戏"; return <><div className="canvas-shell game-2048-shell" tabIndex={0} aria-label={`${config.title}数字方块棋盘`} onPointerDown={(event) => { start.current = { x: event.clientX, y: event.clientY }; }} onPointerUp={end} style={{ background: visual.floor, borderColor: visual.grid }}><div className="canvas-hud"><div><span>得分</span><b style={{ color: visual.hud }}>{state.score}</b></div><div><span>目标</span><b style={{ color: visual.hud }}>{config.game2048.targetTile}</b></div><div><span>状态</span><b style={{ color: visual.hud }}>{state.status === "running" ? "进行中" : "准备"}</b></div></div><div className="game-2048-grid">{get2048Cells(state.board).map(({ x, y, value }) => <div key={`${x}-${y}`} className="game-2048-tile" style={{ background: color(value, visual.collectible), color: value >= 128 ? "#fff" : visual.player }}>{value || ""}</div>)}</div>{state.status === "running" && <button className="canvas-pause" type="button" onClick={() => setState((current) => ({ ...current, status: "paused" }))}>Ⅱ 暂停</button>}{state.status !== "running" && <div className="game-overlay"><h2>{title}</h2><p>{state.status === "lost" ? "棋盘已无法合并，重新挑战吧。" : state.status === "won" ? "你成功合成了目标数字！" : "方向键 / WASD / 手势滑动均可操作。"}</p><button type="button" className="button button-primary" onClick={() => state.status === "paused" ? setState((current) => ({ ...current, status: "running" })) : reset(true)}>{state.status === "paused" ? "继续游戏" : state.status === "ready" ? "开始游戏" : "重新开始"}</button></div>}</div><div className="touch-controls" aria-label="2048 触屏方向控制"><button aria-label="向上滑动" onClick={() => move("up")}>↑</button><div><button aria-label="向左滑动" onClick={() => move("left")}>←</button><button aria-label="向下滑动" onClick={() => move("down")}>↓</button><button aria-label="向右滑动" onClick={() => move("right")}>→</button></div></div></>; }
